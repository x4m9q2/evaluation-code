Place external model weights here before running the reproduction scripts.

Expected relative paths are documented in the top-level `README.md` and
`docs/LLAVA_SAGE_REPRO.md`. Model weights are intentionally excluded from this
repository.

